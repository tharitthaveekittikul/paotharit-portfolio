---
title: "SAT Scan Report: Security Vulnerability Reporting Pipeline"
description: "เครื่องมือภายในที่พัฒนาที่ SCB Tech X สำหรับแปลงผลสแกนจาก Blackduck, Coverity และ Aquasec ใน Jenkins pipeline ให้เป็นรายงาน Excel พร้อมส่งอนุมัติ HQ อัตโนมัติ"
date: 2023-06-01
type: project
status: published
featured: false
tags: [security, blackduck, coverity, aquasec, jenkins, excel, internship, scb]
techStack: [react, springboot, jenkins, blackduck, coverity, aquasec]
role: "Intern Developer"
duration: "2023"
projectStatus: "Internal Tool / Completed"
---

## บริบท

SAT Scan เป็นหนึ่งในเครื่องมือภายในที่พัฒนาโดย SCB Tech X ซึ่งเป็นหน่วยงานเทคโนโลยีของกลุ่ม SCB โดยอยู่ร่วมกับระบบภายในอื่นๆ เช่น Timesheet และ OT System แยกจากผลิตภัณฑ์สำหรับลูกค้า (SCB Easy, Just4U, SCB Connect) ที่อยู่ภายใต้ SCB X

<figure>
  <ZoomableImage
    src="/projects/sat-scan-report/overview-SCBx.png"
    alt="SCB X organization overview showing SAT SCAN as an internal SCB Tech X tool"
    className="border border-zinc-200 dark:border-zinc-800"
  />
  <figcaption className="mt-1 text-center text-xs text-zinc-500">
    SCB X ecosystem — SAT SCAN อยู่ในกลุ่มเครื่องมือภายในของ SCB Tech X
  </figcaption>
</figure>

## ปัญหา

ทุกครั้งที่ SCB Tech X จะปล่อย release ซอฟต์แวร์ต้องผ่านด่านตรวจสอบด้านความปลอดภัยก่อนที่ SCB สำนักงานใหญ่จะอนุมัติ Jenkins pipeline รัน scanner 3 ตัวอยู่แล้ว ได้แก่ Blackduck, Coverity และ Aquasec แต่ละตัวสร้าง output ในรูปแบบ PDF, CSV และ HTML ปัญหาคือขั้นตอนถัดไป — ต้องมีคนนั่งคัดลอกข้อมูลจากไฟล์เหล่านั้นมาใส่ spreadsheet ด้วยมือ จัดระดับความรุนแรงแต่ละรายการ แล้วจัดรูปแบบเป็น Excel เพียงไฟล์เดียวให้ทีม HQ ดำเนินการ ด้วยหลายโปรเจกต์และรอบ release ที่ถี่ ขั้นตอนนี้ช้า เกิดข้อผิดพลาดง่าย และทำให้กระบวนการอนุมัติติดขัด

## Pipeline การสแกน

ทุก deployment เริ่มจากโค้ด นักพัฒนา push การเปลี่ยนแปลง Jenkins pipeline รัน และเมื่อสิ้นสุด build scanner 3 ตัวจะสร้างไฟล์รายงานของตัวเอง — ซึ่งเป็นวัตถุดิบที่ SAT Scan Report รับช่วงต่อ

<figure>
  <ZoomableImage
    src="/projects/sat-scan-report/flow-diagram-system-pre-data.png"
    alt="Flow showing development to Jenkins pipeline deployment to scan reports"
    className="border border-zinc-200 dark:border-zinc-800"
  />
  <figcaption className="mt-1 text-center text-xs text-zinc-500">
    Development → Jenkins pipeline → รายงานจาก Coverity, Blackduck และ Aquasec
  </figcaption>
</figure>

| Scanner   | ตรวจสอบอะไร                                | รูปแบบ output |
| --------- | ------------------------------------------ | ------------- |
| Blackduck | ช่องโหว่ใน open-source dependencies (CVEs) | PDF / CSV     |
| Coverity  | Static analysis — จุดอ่อนในโค้ด (CWEs)     | HTML          |
| Aquasec   | ช่องโหว่ใน container image (CVEs)          | JSON / HTML   |

<figure>
  <ZoomableImage
    src="/projects/sat-scan-report/data-flow.png"
    alt="Data flow: Coverity PDF, Blackduck PDF/CSV, Aquasec JSON/HTML as inputs to Excel report sent to Security Team SCB"
    className="border border-zinc-200 dark:border-zinc-800"
  />
  <figcaption className="mt-1 text-center text-xs text-zinc-500">
    Input: สามรูปแบบ scanner → Output: Excel ไฟล์เดียว → Security Team SCB
  </figcaption>
</figure>

## วิธีการทำงาน

วิศวกรล็อกอิน (email/password หรือ Microsoft SSO) เลือกโปรเจกต์และเวอร์ชัน release แล้วอัปโหลดไฟล์ output จาก scanner แอปจะ parse แต่ละไฟล์ — ดึง CVE ID, CWE ID, ชื่อ library, เวอร์ชัน, คะแนน severity — แล้วเติมข้อมูลลงตารางโดยอัตโนมัติ ไม่ต้องคัดลอกด้วยมือ

<figure>
  <ZoomableImage
    src="/projects/sat-scan-report/authenticate-page.png"
    alt="SAT Scan Report sign-in page with Microsoft SSO option"
    className="border border-zinc-200 dark:border-zinc-800"
  />
  <figcaption className="mt-1 text-center text-xs text-zinc-500">
    หน้า Sign in — ล็อกอินด้วย email/password หรือ Microsoft SSO
  </figcaption>
</figure>

<figure>
  <ZoomableImage
    src="/projects/sat-scan-report/sat-scan-flow-1.png"
    alt="Flow showing project selection then Coverity file upload dialog and parsed table"
    className="border border-zinc-200 dark:border-zinc-800"
  />
  <figcaption className="mt-1 text-center text-xs text-zinc-500">
    เลือกโปรเจกต์และ release แล้วอัปโหลดไฟล์ scanner — แยก tab ต่อ scanner
  </figcaption>
</figure>

<figure>
  <ZoomableImage
    src="/projects/sat-scan-report/sat-scan-flow-2.png"
    alt="Flow showing Blackduck tab with suggested available fix and editable remark column"
    className="border border-zinc-200 dark:border-zinc-800"
  />
  <figcaption className="mt-1 text-center text-xs text-zinc-500">
    Tab Blackduck — เวอร์ชันที่แนะนำแสดง inline; remark แก้ไขได้โดยตรงในตาราง
  </figcaption>
</figure>

## Scanner 3 ตัว มุมมองเดียว

ตารางหลักแบ่งเป็น tab ตาม scanner แต่ละ tab แสดงผลที่ดึงมาจากไฟล์รายงานของเครื่องมือนั้น พร้อม column สำหรับ image name, library/version, ชื่อช่องโหว่, severity, status, remark และ compensation control

<figure>
  <ZoomableImage
    src="/projects/sat-scan-report/table-scan-example.png"
    alt="SAT Scan Coverity tab showing file upload dialog and parsed CWE findings table"
    className="border border-zinc-200 dark:border-zinc-800"
  />
  <figcaption className="mt-1 text-center text-xs text-zinc-500">
    Tab Coverity — อัปโหลดไฟล์ scan แล้วผลลัพธ์จะเติมลงตารางอัตโนมัติ
  </figcaption>
</figure>

### Blackduck

<figure>
  <ZoomableImage
    src="/projects/sat-scan-report/extraction-pdf-example.png"
    alt="Example of PDF report file being uploaded and extracted"
    className="border border-zinc-200 dark:border-zinc-800"
  />
  <figcaption className="mt-1 text-center text-xs text-zinc-500">
    PDF จาก scanner — แอปดึงแถวช่องโหว่ออกมาอัตโนมัติ
  </figcaption>
</figure>

<figure>
  <ZoomableImage
    src="/projects/sat-scan-report/blackduck-output-example.png"
    alt="Blackduck output table showing hundreds of library/version/severity rows extracted"
    className="border border-zinc-200 dark:border-zinc-800"
  />
  <figcaption className="mt-1 text-center text-xs text-zinc-500">
    Blackduck output — ดึง library มากกว่า 200 แถวพร้อม review
  </figcaption>
</figure>

<figure>
  <ZoomableImage
    src="/projects/sat-scan-report/blackduck-report-example.png"
    alt="How Blackduck scan data maps to Excel report columns showing component, version and severity"
    className="border border-zinc-200 dark:border-zinc-800"
  />
  <figcaption className="mt-1 text-center text-xs text-zinc-500">
    Blackduck → Excel mapping — component, version, severity (C/H/M/L) map ลงใน column ของรายงาน
  </figcaption>
</figure>

<figure>
  <ZoomableImage
    src="/projects/sat-scan-report/dependencies-report-example.png"
    alt="Dependencies report showing library vulnerabilities"
    className="border border-zinc-200 dark:border-zinc-800"
  />
  <figcaption className="mt-1 text-center text-xs text-zinc-500">
    Blackduck dependency scan — CVE ของ open-source library จัดกลุ่มตาม component
  </figcaption>
</figure>

### Coverity

<figure>
  <ZoomableImage
    src="/projects/sat-scan-report/coverity-output-example.png"
    alt="Coverity output showing CWE findings extracted from static analysis"
    className="border border-zinc-200 dark:border-zinc-800"
  />
  <figcaption className="mt-1 text-center text-xs text-zinc-500">
    Coverity output — CWE ที่ดึงจาก static analysis
  </figcaption>
</figure>

<figure>
  <ZoomableImage
    src="/projects/sat-scan-report/coverity-report-example.png"
    alt="How Coverity CWE data maps to Excel report columns showing CWE ID and technical impact"
    className="border border-zinc-200 dark:border-zinc-800"
  />
  <figcaption className="mt-1 text-center text-xs text-zinc-500">
    Coverity → Excel mapping — CWE ID และ technical impact map ลง column ของรายงาน
  </figcaption>
</figure>

<figure>
  <ZoomableImage
    src="/projects/sat-scan-report/cwe-report-example.png"
    alt="Coverity CWE report showing static analysis findings"
    className="border border-zinc-200 dark:border-zinc-800"
  />
  <figcaption className="mt-1 text-center text-xs text-zinc-500">
    Coverity static analysis — CWE ที่พบจากการสแกน source code
  </figcaption>
</figure>

### Aquasec

<figure>
  <ZoomableImage
    src="/projects/sat-scan-report/aquasec-output-example.png"
    alt="Aquasec output showing container image CVE findings extracted"
    className="border border-zinc-200 dark:border-zinc-800"
  />
  <figcaption className="mt-1 text-center text-xs text-zinc-500">
    Aquasec output — CVE ของ container image ที่ดึงออกมา
  </figcaption>
</figure>

<figure>
  <ZoomableImage
    src="/projects/sat-scan-report/aquasec-report-example.png"
    alt="How Aquasec scan data maps to Excel report columns"
    className="border border-zinc-200 dark:border-zinc-800"
  />
  <figcaption className="mt-1 text-center text-xs text-zinc-500">
    Aquasec → Excel mapping — severity CVE และรายละเอียด container map ลง column ของรายงาน
  </figcaption>
</figure>

<figure>
  <ZoomableImage
    src="/projects/sat-scan-report/cve-report-example.png"
    alt="Aquasec CVE scan report showing vulnerability severity levels"
    className="border border-zinc-200 dark:border-zinc-800"
  />
  <figcaption className="mt-1 text-center text-xs text-zinc-500">
    Aquasec container scan — แบ่งระดับ Critical, High, Medium, Low, Negligible ต่อ resource
  </figcaption>
</figure>

## การจัดระดับความรุนแรง

แต่ละรายการถูกจัดระดับเป็นหนึ่งใน 4 ระดับ: **Critical**, **High**, **Medium** หรือ **Low** ทีมใช้ข้อมูลนี้ตัดสินใจว่าช่องโหว่ไหนต้องแก้ก่อน release จะได้รับอนุมัติ, ยอมรับได้โดยมี compensation control ที่บันทึกไว้ หรือเลื่อนไปได้ column remark และ compensation control แก้ไขได้โดยตรงในตาราง — วิศวกรกรอกระหว่างการ review ไม่ต้องทำใน spreadsheet แยก

## ประสิทธิภาพ

ก่อนที่ระบบนี้จะมี การกรอกรายงานความปลอดภัยจากต้นทางหมายถึงการคัดลอกแต่ละรายการด้วยมือ — ประมาณ 20 วินาทีต่อแถว เมื่อนับจำนวนแถวเฉลี่ยต่อ scanner จะได้:

| Scanner   | แถวเฉลี่ย/sheet | เวลาที่ใช้ด้วยมือ/sheet |
| --------- | --------------- | ----------------------- |
| Coverity  | 50              | ~20 นาที                |
| Blackduck | 250             | ~1 ชั่วโมง 40 นาที       |
| Aquasec   | 10              | ~3 นาที                 |

SAT Scan Report กำจัดขั้นตอนนั้นออกทั้งหมด — อัปโหลดไฟล์ ตารางเติมอัตโนมัติ แล้ว export ได้เลย ทั้งสาม scanner เสร็จภายในไม่กี่นาที สำหรับการประมวลผลแบบ bulk ระบบรองรับ 100 ไฟล์ Blackduck ในประมาณ 16 วินาที

<figure>
  <ZoomableImage
    src="/projects/sat-scan-report/100-bulk-blackduck-files-show-performance.png"
    alt="Performance test showing 100 Blackduck files processed in approximately 16 seconds"
    className="border border-zinc-200 dark:border-zinc-800"
  />
  <figcaption className="mt-1 text-center text-xs text-zinc-500">
    100 ไฟล์ Blackduck bulk-mapped ในประมาณ 16 วินาที
  </figcaption>
</figure>

## สร้าง Excel และการอนุมัติ HQ

เมื่อทีม review ช่องโหว่ทุกรายการจากทุก scanner tab แล้ว กด "Generate Excel" จะ export การ review ทั้งหมดเป็นไฟล์ XLSX ไฟล์นั้นคือสิ่งที่ส่งต่อ — senior ตรวจสอบก่อน แล้วจึงส่งไปยังทีมความปลอดภัยของ SCB สำนักงานใหญ่เพื่ออนุมัติขั้นสุดท้าย

<figure>
  <ZoomableImage
    src="/projects/sat-scan-report/user-diagram.png"
    alt="Use case diagram showing all user actions in SAT Scan Report System"
    className="border border-zinc-200 dark:border-zinc-800"
  />
  <figcaption className="mt-1 text-center text-xs text-zinc-500">
    Use case diagram — ขอบเขตการใช้งานทั้งหมดตั้งแต่ authentication จนถึง Excel export
  </figcaption>
</figure>

## สิ่งที่ได้เรียนรู้

นี่คือโปรเจกต์ internship แรกที่มีข้อจำกัดเชิง enterprise จริงๆ — ผู้ใช้ภายในองค์กร, ขั้นตอนการอนุมัติที่เป็นทางการ, และรายงานที่มีผลกระทบด้าน compliance โดยตรง ส่วนที่น่าสนใจที่สุดคือ layer การ parse ไฟล์ แต่ละ scanner มีรูปแบบและโครงสร้าง output ต่างกัน ดังนั้น logic การดึงข้อมูลต้องรองรับหลาย layout โดยไม่มี schema กลาง การสร้างให้ทำงานได้อย่างน่าเชื่อถือและไม่มีข้อมูลหลุดโดยไม่รู้ตัว ต้องใช้ความระมัดระวังมากกว่าการทำ UI เสียอีก
